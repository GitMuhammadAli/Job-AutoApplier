# ── Database ──
DATABASE_URL="postgresql://user:pass@host:5432/jobpilot?sslmode=require"

# ── NextAuth ──
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXT_PUBLIC_APP_URL="https://your-domain.vercel.app"

# ── OAuth Providers ──
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ── Email (Brevo SMTP) ──
SMTP_HOST="smtp-relay.brevo.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
NOTIFICATION_EMAIL=""

# ── Cron Secret (protects /api/cron/* endpoints) ──
CRON_SECRET="your-secret-here"

# ── Job Source APIs ──
RAPIDAPI_KEY=""          # JSearch — https://rapidapi.com/letscrape-6bRBa3QguO5/api/jsearch
ADZUNA_APP_ID=""         # Adzuna — https://developer.adzuna.com/
ADZUNA_APP_KEY=""
ADZUNA_COUNTRY=""           # Optional — defaults to "us" if empty
SERPAPI_KEY=""            # Google Jobs — https://serpapi.com/

# ── AI ──
GROQ_API_KEY=""          # Cover letters — https://console.groq.com/keys

# ── Webhooks ──
BREVO_WEBHOOK_SECRET=""  # Brevo email bounce webhook signature verification

# ── Security ──
ENCRYPTION_KEY=""        # SMTP password encryption — generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ── Blob Storage ──
BLOB_READ_WRITE_TOKEN="" # Vercel Blob — auto-set on Vercel, needed for resume uploads

# ── Admin ──
ADMIN_EMAILS="your-email@gmail.com" # Comma-separated list of admin emails (OAuth-based admin access)
ADMIN_USERNAME="jobpilot"            # Separate admin panel credentials
ADMIN_PASSWORD="change-this-password" # Separate admin panel password
